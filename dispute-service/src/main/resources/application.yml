server:
  port: 8085
spring:
  application:
    name: dispute-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dispute_db}
    username: ${DB_USER:immo}
    password: ${DB_PASS:immo_secret}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
